import React, { FC } from 'react';
import styled from 'styled-components';

const PlanInfo: FC = () => {
    return <Container>PlanInfo</Container>;
};

export default PlanInfo;

const Container = styled.div`
    width: 100%;
    height: 100px;
    background-color: pink;
`;
