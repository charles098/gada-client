import React, { FC } from 'react';
import styled from 'styled-components';

const PlaceOption: FC = () => {
    return <Container>PlaceOption</Container>;
};

export default PlaceOption;

const Container = styled.div`
    width: 100%;
    height: 180px;
    background-color: green;
`;
