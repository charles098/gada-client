import React, { FC } from 'react';
import styled from 'styled-components';

const PlanMaker: FC = () => {
    return <Container>PlanMaker</Container>;
};

export default PlanMaker;

const Container = styled.div`
    width: 1000px;
    height: 850px;
    background-color: purple;
`;
